# IQ4_XS + agentic imatrix(v2) + MTP + SM70
# 与 q5 档的差别: 权重档位(4.25bpw vs 5.33bpw)、页池预算(+4000MB)、
#                 独立的日志与前缀缓存目录。
# fastllm 的 SM70 IQ4_XS 走 DP4A MMQ(src/devices/cuda/fastllm-iq4xs-sm70.cu),
# 默认开启, FASTLLM_CUDA_SM70_IQ4XS_MMQ=0 可关。
#
# 关于速度: IQ4_XS 在 V100 上**理论上应该比 Q5_K_M 更快**, 不是"可能更慢"。
#   27B 的 decode 是**显存带宽瓶颈**而非算力瓶颈: 每生成一个 token 要把全部
#   权重读一遍, Q5_K_M 约 20.8GB vs IQ4_XS 约 16.5GB —— 少读 26%。
#   V100 是 900GB/s HBM2, 这 26% 直接反映在 decode tok/s 上。
#   常见的"IQ 查表开销大所以慢"在这里不成立: SM70 走的是 DP4A MMQ
#   (src/devices/cuda/fastllm-iq4xs-sm70.cu), 把激活量化成 int8 后**直接在
#   量化权重上做整数点积, 不做完整反量化**, 额外 ALU 开销被带宽瓶颈掩盖。
#
# ---------------------------------------------------------------------------
# 2026-08-20 更正: 上面那段"SM70 走 DP4A MMQ"是**错的**, 已实测结案。
#
#   decode(n=1..3) 走的是 gguf.mmvq, **不是** fastllm-iq4xs-sm70.cu 的 MMQ。
#   这不是缺陷: MMQ 的耗时对 n 几乎不变(小 n 被 pad 到 tile 宽 8), 交叉点在
#   n≈6~8, 强行让 n=1..3 进 MMQ 会慢 1.6~4.3 倍。所以
#   `curl /props | jq .kernel_routes` 里 gguf.sm70_iq4xs_mmq 只在 n=13..59
#   出现、decode 段恒为 0, **是正确的观测结果**。
#
#   而 mmvq 的 IQ4_XS 分支本来就是量化点积(vec_dot_iq4_xs_q8_1, 查表+dp4a,
#   不写反量化中间缓冲), 空闲 GPU 上实测 735 GB/s = V100 900 GB/s 的 82%,
#   且在全部 30 组(生产形状 x n=1/2/3)上都比 Q5_K 的 mmvq 快(耗时比 0.63~0.92)。
#   端到端 8 组完全匹配的探针样本: IQ4_XS 比 Q5_K_M 快 1.20x。
#
#   ——所以"实测 decode 更慢"这个前提本身没成立过, 之前那个 5.9 vs 6.9~12.1
#     是**非受控对比**(上下文长度不同 + 下面那两个开关不同 + 二进制不同)。
#     详见 v100-perfs/docs/EXPERIENCE.md §15.35 第 8 条。
#
# 【做 A/B 时必读】本档相对 q38-PROD-cyber-q5-*.env **不止换了权重**, 还差:
#     FASTLLM_CUDA_PAGED_CUBLAS_BATCH_GQA   (本档 =1, q5 档未设即 0)
#     FASTLLM_PAGED_POOL_MAX_MB             (10600 vs 6600)
#   要比"IQ4_XS vs Q5_K_M", 必须先把这两项对齐, 否则量到的是三个变量的和。
# ---------------------------------------------------------------------------
#   省出的 4GB 是实打实的可用页, 直接缓解当天反复出现的 Grow 失败。
#
#   但要说清楚: **这 4GB 不是解药, 只是缓解**。262K 上下文的 KV 本身远不止
#   4GB, 省这点不改变量级关系。真正的结构性优化在别处(SM70 注意力实现、
#   KV 足迹、前缀缓存命中率、prefill 调度), 见 v100-perfs/docs 的相关条目。
# model=c-iq4xs+imatrix weights~15.0GiB kv=turbo3 mtp=1 ctx=262144 batch=1 cache=base sm70=on uncensored=yes
PROJECT_DIR=/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/v100-perfs
PROXY_HOST=0.0.0.0
PROXY_PORT=8000
PROXY_LOG_FILE=/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/v100-perfs/runtime/fastllm-native-profiles/logs/proxy-PROD-cyber-iq4xs.log
FASTLLM_BACKEND_URL=http://127.0.0.1:8002
FASTLLM_CHAT_TEMPLATE=/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/models/_official_qwen38_refs/Qwen3.8-27B/chat_template.jinja
FASTLLM_MODEL_SLUG=qwen3.8-fastllm
FASTLLM_PUBLIC_ALIASES=qwen3.8-27b,qwen3.8-27b-q4kxl,qwen3.8-27b-heretic
FASTLLM_OWNED=1
FASTLLM_BACKEND_COMMAND='/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/fastllm/build-rw/apiserver --path /run/media/ezra/13D010B6FDBC1A06/1CatVLLM/models/Qwen3.8-27B-Uncensored-Cyber-GGUF/imatrix/Qwen3.8-27B-Uncensored-Cyber-IQ4_XS-imatrix-fromq8-plus-mtp.gguf --mmproj /run/media/ezra/13D010B6FDBC1A06/1CatVLLM/models/Qwen3.8-27B-Uncensored-Cyber-GGUF/mmproj-Qwen3.8-27B-Uncensored-Cyber-Q8_0.gguf --threads 2 --atype float16 --kv_cache_dtype turbo3 --batch 4 --tokens 262144 --default_max_tokens 65536 --model_name qwen3.8-fastllm --port 8002 --device cuda'
FASTLLM_BACKEND_CWD=/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/v100-perfs
FASTLLM_BACKEND_LOG=/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/v100-perfs/runtime/fastllm-native-profiles/logs/backend-PROD-cyber-iq4xs.log
FASTLLM_START_TIMEOUT=1800
FASTLLM_STOP_TIMEOUT=30
FASTLLM_IDLE_TIMEOUT=0
FASTLLM_LIFECYCLE_INTERVAL=5
FASTLLM_VRAM_MIN_FREE_GIB=1.5
FASTLLM_VRAM_RESUME_FREE_GIB=2.5
QUEUE_TIMEOUT=3600
FALLBACK_ENABLED=0
FASTLLM_PREFIX_CACHE_PERSIST=1
FASTLLM_PREFIX_CACHE_DISK_DIR=/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/v100-perfs/runtime/prefix-cache-q38-prod-cyber-iq4xs
# PERSIST_KEY 移除: 权重指纹自动派生
#FASTLLM_PREFIX_CACHE_PERSIST_KEY=q38-prod-cyber-q5
FASTLLM_PREFIX_CACHE_DISK_MIN_FREE_BYTES=8589934592
FASTLLM_CPU_REQUEST_SWAP=1
FASTLLM_CPU_REQUEST_SWAP_ZSTD=1
FASTLLM_CPU_REQUEST_SWAP_ZSTD_LEVEL=3
FASTLLM_CPU_REQUEST_SWAP_DISK_DIR=/run/media/ezra/13D010B6FDBC1A06/1CatVLLM/v100-perfs/runtime/swap-q38-prod-cyber-iq4xs
FASTLLM_CPU_REQUEST_SWAP_DISK_MAX_BYTES=4294967296
FASTLLM_CPU_REQUEST_SWAP_DISK_MIN_BYTES=67108864
FASTLLM_CPU_REQUEST_SWAP_DISK_MIN_FREE_BYTES=4294967296
FASTLLM_QWEN35_ENABLE_MTP=2
FASTLLM_QWEN35_INTERLEAVE_LONG_PREFILL=1
FASTLLM_HOST_SUSPEND_CACHE=1
FASTLLM_HOST_SUSPEND_CACHE_MAX_BYTES=17179869184
FASTLLM_HOST_SUSPEND_MIN_FREE_BYTES=4294967296
FASTLLM_SKIP_WARMUP=1
FASTLLM_PREFIX_CACHE_STATS=1
FASTLLM_TOOLCALL_GRAMMAR=1
CUDA_VISIBLE_DEVICES=0
FASTLLM_QWEN35_TURBO3_KV=1
FASTLLM_QWEN35_TURBO4_KV=0
# 前缀缓存磁盘层配额。2026-08-20 从 2GiB 提到 32GiB。
# 原值 2147483648(2GiB) 太小: 活跃 root 已占 1.88GiB, 只剩 262MB 余量,
# 而 262K 上下文的 checkpoint 远大于此 -> 每次关机存档都失败:
#   [Prefix-persist] shutdown checkpoint failed:
#   persistent prefix cache disk byte limit exceeded   (日志里连续 6 次)
# 后果是**每次重启前缀缓存全丢**, 磁盘层形同虚设。
# 注意源码里那个"配额满->提交失败->回收不执行->永远满"的自锁 bug 已经修过
# (prefixcache_persistence.cpp:795 附近会先 Prune 历史 generation 再判),
# 但回收只能在活跃 root 内清 generation, 清不出这么大的空间 —— 是配额数字
# 本身不合理, 不是回收逻辑的问题。磁盘实际还有 2.0TB 空闲。
# 下限保护由 FASTLLM_PREFIX_CACHE_DISK_MIN_FREE_BYTES(8GiB) 负责, 不会撑爆盘。
FASTLLM_PREFIX_CACHE_DISK_MAX_BYTES=34359738368
FASTLLM_PREFIX_CACHE_SNAPSHOT_INTERVAL_PAGES=4
FASTLLM_PREFIX_CACHE_CPU_TIER=1
FASTLLM_PREFIX_CACHE_CPU_MAX_BYTES=17179869184  # 3GiB->16GiB, 理由见 q5 档 profile

# ---------------------------------------------------------------------------
# 多模态(vision)请求的前缀缓存开关。默认 1(开)。
#
# 背景: 2026-08-20 之前, 带图请求的 token 级命中率**恒为 0** —— 不是调优问题,
# 是 ForwardMultimodal -> ForwardFromHiddenStates 把分页 KV manager 的全局层号
# 硬编码成 i*2(基址 0), 而纯文本走 ForwardGPU 用的是 threadTpPagedCacheBase+i
# (启动时被定成 3000000+), 前缀缓存查询侧只认后者。两套 manager 互不相交:
# 前缀链记进了永远不会被查询的一组, 查询落在从没记过这条前缀的另一组。
# 现场特征: 同一后端纯文本命中 94%(hit=1024/1095), 所有带图请求(48K~85K token)
# 全部 hit=0; 而本机 agent 流量 100% 带图。已由 PagedCacheLayerBase() 修复。
#
# 【什么情况下必须把这个开关关成 0】
# trie 的 key 是 **token id 序列**, 而图片在序列里只是一串完全相同的
# <|image_pad|> 占位符 —— 图像**内容不进 key**。因此只要出现下面这种用法,
# 就会静默复用到另一张图算出来的 KV(不报错、不崩溃, 只是答非所问):
#
#   * 同一会话内**替换图像内容**、而**周围文本不变**。典型形态:
#       - 每轮把同一个位置的截图换成新截图, 提示词模板一字不改;
#       - 定时轮询同一个摄像头/仪表盘画面, 用固定的 user 文本追问;
#       - 图像编辑/对比类工作流: "看这张图" -> 换图 -> 再问同一句话。
#     只要新旧图的**分辨率相同**(=> image_pad token 数相同), token 序列
#     完全一致, trie 会一路匹配过去, 复用上一张图的 KV。
#   * 多个不同会话共用同一套 system prompt + 同一句 user 文本, 只是图不同。
#
# 【为什么本机保持默认开启是安全的】
# 本机负载是 agent 循环: 每轮把历史**原样重发**, 前缀里的图就是同一批图,
# 新图只追加在末尾。实测日志中同一 agent 每轮的
#   [Qwen3.5 vision] aggregate image pixels capped: images=5 original=... resized=...
# 三个数字逐轮完全相同, 即历史图像未被替换。收益是 100% 真实流量从 0% 命中
# 变成可命中。
#
# 【2026-08-20 12:20 暂时改成 0; 12:45 更正了理由 —— 原来的理由是错的】
#
# 更正: 我最初报的"带图请求命中前缀 -> imageGridThwList 断言 -> 确定性 500"
# 是**读错了代码**。ForwardMultimodal 开头有一个"续算"分支:
#     if (pastKeyValues.size() > 0 && pastKeyValues[0].second.dims.size() > 0)
# 命中恢复时 RestorePagedPrefixCacheExtra 会给线性层(含 layer 0)写上 dims,
# 所以这个条件成立, 走的是续算分支 —— 它不做视觉编码也不重算 mm 元数据,
# 因此**不会**触发那个断言, **不会** 500。那条分支的注释本来就写着
# "续 prefill(典型为前缀缓存 HIT 之后)"。
#
# 仍然保持 0 的理由(读代码得出, **尚未在运行时复现**, 属于待验证):
#   (a) mrope_position_delta 只在 ForwardMultimodal 的视觉分支里产生,
#       PrepareMultimodalImageInputs 不产出它。前缀命中时该请求的视觉分支
#       从未执行 -> delta 缺失 -> adjustedPositionIds 退化成纯顺序位置,
#       没有 mRoPE 修正(含图 prompt 的 delta 通常非 0)。
#   (b) 落在未命中残段里的图片, 这条路上不会被 EncodeVisualItems 编码,
#       模型看到的是 <|image_pad|> 的普通 embedding, 不是图像特征。
# 两条都是**静默**的, 所以在实测证伪或修好之前保持关闭。
#
# 改回 1 的判据: 用一个**真的能把图片送进模型**的两轮 agent 探针
# (注意: scripts/probe_prefix_multimodal.py 当前因 Jinja 模板报错退化成纯文本,
#  是假阳性源, 先修它), 对比开关 on/off 两次的输出是否一致, 且第 2 轮 hit>0。
#
# 【这只是逃生阀, 不是终局方案】
# 正解是把每个页块覆盖到的图像内容哈希折进页哈希(vLLM 的做法), 之后本开关
# 可以删除。见 fastllm/UPSTREAM_BUMP_CHECKLIST.md "待办" 一节。
FASTLLM_PREFIX_CACHE_MULTIMODAL=1
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# 2026-08-20 审计结论: 下面这两行在当前 KV 档位下是 **no-op**, 不是"关掉了优化"。
#
#   FASTLLM_CUDA_SM70_PAGED_XQA   -> FastllmCudaTrySm70PagedAttentionDecode
#     入口要求分页 K/V **都是 float16**
#     (fastllm/src/devices/cuda/attention/paged/fastllm-paged-attention-native.cu
#      的 pagedK->dataType != FLOAT16 直接 return false)
#   FASTLLM_CUDA_SM70_FLASH_ATTN  -> FastllmCudaTrySm70FlashAttentionPrefill
#     入口要求分页 K/V **都是 fp8_e4m3**, 且 kvLen<=512、每请求 qLen 2..10
#     (fastllm/src/devices/cuda/attention/paged/fastllm-sm70-flash-attention.cu)
#
# 本 profile 用 turbo3 => K=q8_0, V=turbo3。两个条件一个都不满足, 所以这两条
# 路径**取 0 还是取 1, 运行期完全一样**。实证: sweep 的 c1(两个都=0)与
# c6(两个都=1)的后端日志逐行相同, 都是
#     [Fastllm] FlashInfer attention disabled on GPU 0 (CC 7.0), using native attention.
#     [Fastllm] Native paged attention uses chunked cublas attention.
# 全部 backend 日志里 grep XQA / "SM70 flash" 零命中。
#
# 由此产生的两处需要修正的记录:
#   1. 本文件名带 "-sm70" 后缀、第 1 行注释写 "SM70 算子", docs/EXPERIENCE.md
#      写 "SM70 开" —— 与实际的 0/0 矛盾, 而且就算改成 1/1 也一样没生效。
#   2. docs/EXPERIENCE.md 把 "c6 的 MTP 接受率 95.1/86.8% vs c1 的 76.3/56.2%"
#      归因给 SM70 算子。既然两组跑的是同一条 kernel, **这个归因不成立**,
#      那个差异来自别的变量或噪声, 选型依据需要重新验证。
#
# FLASH_ATTN=0 另有独立的正当理由(docs/benchmarks/fastllm-qwen36-legacy.md:345,353):
#   该实验路线"端到端输出与 native 不等价"(token 数和 SHA-256 都不同), 且更慢
#   (1 agent steady 10.00 -> 4.64 tok/s)。所以它**不该**被打开, 即使将来换成
#   fp8_e4m3 KV 也要先解决数值等价问题。
#
# XQA 则相反 —— 它是有真实收益的(单层 microbench 8K/32K/128K 上 2.22x/3.37x/4.03x,
# docs/benchmarks/fastllm-qwen36-legacy.md:337-339), 只是要 float16 分页 KV。要不要为它放弃
# turbo3 的 KV 压缩, 是一个**显存 vs 注意力速度**的取舍, 262K 上下文下多半不划算。
# 想验证时: 启动后 grep 日志里的 [Fastllm][sm70-attn] 一行, 或
#   curl -s 127.0.0.1:8002/props | jq .kernel_routes
# 看 attn.sm70_paged_xqa 是不是 0。
# ---------------------------------------------------------------------------
FASTLLM_CUDA_SM70_FLASH_ATTN=0
FASTLLM_CUDA_SM70_PAGED_XQA=0
PROXY_STREAM_SLOTS=4
MAX_QUEUE_WAITERS=64
# 页池预算: 从 6600 提到 10600(+4000)。
# 依据: IQ4_XS 约 4.25bpw vs Q5_K_M 约 5.33bpw, 27B 权重从约 20.8GB 降到约
# 16.5GB, 省出约 4GB。这 4GB 必须显式划给页池, 否则只是空着 —— 而当天所有
# 事故的根子都是页池不够:
#   - pool 顶到 6590/6600MB, Grow 抛异常
#   - Grow 异常穿到 MTPLoop catch, 把所有在飞请求 isAbort
#   - 前缀缓存被当唯一泄压阀反复吃空(L1trie 恒为 0)
# 切换后要盯: kv_pool 占比是否明显下降、L1trie 是否不再归零、hits 是否 >0。
FASTLLM_PAGED_POOL_MAX_MB=10600
FASTLLM_VRAM_POOL_RESERVE_MB=1536
FASTLLM_MTP_LOCK_DEBUG=1

# ---------------------------------------------------------------------------
# 2026-08-20 打开分页注意力的 GQA 批处理。
#
# 不开时 FastllmCudaPagedAttentionNativeChunkedCublasRaw 走
# `for (int g = 0; g < group; g++)` 逐 query-group 循环, 每次都让 cuBLAS 完整
# 读一遍 kChunk/vChunk —— group = 24/4 = 6, 也就是 **K/V 各被重复读 6 遍**。
# 靠 L2 也吃不掉: 默认 chunk=8192 时 kChunk = 8192×256×2 = 4MB, vChunk 同样 4MB,
# 合计 8MB > V100 的 6MB L2, 而且循环 K->V->K->V 交替, 正好互相踢出去。
#
# 每 (token, kvHead) 行的访存账:
#   关: 372(读量化 K+V) + 1024(写连续 fp16) + 6144(重读 6 遍) = 7540 B
#   开: 372 + 1024 + 1024                                    = 2420 B
#
# 代价: batchGqa 分支的 QK 中间矩阵要 group 倍工作区
# (chunkWorkspaceBytes 里 qkGroups = group), 于是二分搜出来的 maxChunk 会变小,
# 分块次数增多。所以这是"少读 6 倍 vs 分块更碎"的取舍, 必须实测而不是推断。
#
# 这个开关默认关着**没有任何注释说明原因**, 随一个笼统的
# "feat: add V100 cache tiers and service controls"(8b6b1ec4) 进来 ——
# 属于未经验证, 而不是已知有问题。所以打开后必须跑一遍验收探针
# (逐字抄写 + 工具名), 输出正确性有任何退化就回退。
#
# 生效条件(fastllm-paged-attention-native.cu:467):
#   qIsHalf && outIsHalf && group > 1
# 本档 --atype float16、group=6, 满足。
FASTLLM_CUDA_PAGED_CUBLAS_BATCH_GQA=1
# 2026-08-20 实测补充(独占 GPU, testTurboPagedAttention --bench, kvLen=8192):
#   qoLen=1: 默认 1.698ms -> BATCH_GQA 1.007ms   = 1.686x
#   qoLen=3: 默认 1.818ms -> BATCH_GQA 1.705ms   = 1.066x   <- 生产稳态是这个
# 也就是说这个开关在 **prefill / qoLen=1** 上值 1.69 倍, 在 **MTP 校验的
# qoLen=3** 上只值 6.6%。生产 MTP=2 时稳态 qoLen≈3(实测 2.92 tokens/call),
# 所以对 decode 的实际收益很小 —— 留着它是因为 prefill 和不合格形状仍受益,
# 且数值等价性已用 fp64 参考验过(与默认路互差 1~2 个 fp16 ULP, 11 组形状,
# 谁更准各占一半)。
#
# **不要**把生产 decode 的任何变化归因给这个开关。同期观察到 decode 从
# 5.9 涨到 9.0 tok/s, 但 1.066x 解释不了 +50%, 那是负载/上下文/前缀缓存状态
# 的变化。这正是本仓反复出现的错误归因模式。
